#--- Mar-03-Homework.log ---#
This is pdfTeX, Version 3.14159265-2.6-1.40.21 (TeX Live 2020/W32TeX) (preloaded format=pdflatex 2021.3.3)  3 MAR 2021 16:35
entering extended mode
 restricted \write18 enabled.
 %&-line parsing enabled.
**Mar-03-Homework.tex
(./Mar-03-Homework.tex
LaTeX2e <2020-10-01> patch level 4
L3 programming layer <2021-02-18> (c:/Users/regen/AppData/Roaming/TinyTeX/texmf
-dist/tex/latex/base/article.cls
Document Class: article 2020/04/10 v1.4m Standard LaTeX document class
(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/base/size10.clo
File: size10.clo 2020/04/10 v1.4m Standard LaTeX file (size option)
)
\c@part=\count179
\c@section=\count180
\c@subsection=\count181
\c@subsubsection=\count182
\c@paragraph=\count183
\c@subparagraph=\count184
\c@figure=\count185
\c@table=\count186
\abovecaptionskip=\skip47
\belowcaptionskip=\skip48
\bibindent=\dimen138
) (c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/lm/lmodern.sty
Package: lmodern 2009/10/30 v1.6 Latin Modern Fonts
LaTeX Font Info:    Overwriting symbol font `operators' in version `normal'
(Font)                  OT1/cmr/m/n --> OT1/lmr/m/n on input line 22.
LaTeX Font Info:    Overwriting symbol font `letters' in version `normal'
(Font)                  OML/cmm/m/it --> OML/lmm/m/it on input line 23.
LaTeX Font Info:    Overwriting symbol font `symbols' in version `normal'
(Font)                  OMS/cmsy/m/n --> OMS/lmsy/m/n on input line 24.
LaTeX Font Info:    Overwriting symbol font `largesymbols' in version `normal'
(Font)                  OMX/cmex/m/n --> OMX/lmex/m/n on input line 25.
LaTeX Font Info:    Overwriting symbol font `operators' in version `bold'
(Font)                  OT1/cmr/bx/n --> OT1/lmr/bx/n on input line 26.
LaTeX Font Info:    Overwriting symbol font `letters' in version `bold'
(Font)                  OML/cmm/b/it --> OML/lmm/b/it on input line 27.
LaTeX Font Info:    Overwriting symbol font `symbols' in version `bold'
(Font)                  OMS/cmsy/b/n --> OMS/lmsy/b/n on input line 28.
LaTeX Font Info:    Overwriting symbol font `largesymbols' in version `bold'
(Font)                  OMX/cmex/m/n --> OMX/lmex/m/n on input line 29.
LaTeX Font Info:    Overwriting math alphabet `\mathbf' in version `normal'
(Font)                  OT1/cmr/bx/n --> OT1/lmr/bx/n on input line 31.
LaTeX Font Info:    Overwriting math alphabet `\mathsf' in version `normal'
(Font)                  OT1/cmss/m/n --> OT1/lmss/m/n on input line 32.
LaTeX Font Info:    Overwriting math alphabet `\mathit' in version `normal'
(Font)                  OT1/cmr/m/it --> OT1/lmr/m/it on input line 33.
LaTeX Font Info:    Overwriting math alphabet `\mathtt' in version `normal'
(Font)                  OT1/cmtt/m/n --> OT1/lmtt/m/n on input line 34.
LaTeX Font Info:    Overwriting math alphabet `\mathbf' in version `bold'
(Font)                  OT1/cmr/bx/n --> OT1/lmr/bx/n on input line 35.
LaTeX Font Info:    Overwriting math alphabet `\mathsf' in version `bold'
(Font)                  OT1/cmss/bx/n --> OT1/lmss/bx/n on input line 36.
LaTeX Font Info:    Overwriting math alphabet `\mathit' in version `bold'
(Font)                  OT1/cmr/bx/it --> OT1/lmr/bx/it on input line 37.
LaTeX Font Info:    Overwriting math alphabet `\mathtt' in version `bold'
(Font)                  OT1/cmtt/m/n --> OT1/lmtt/m/n on input line 38.
)
(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/amsmath/amsmath.st
y
Package: amsmath 2020/09/23 v2.17i AMS math features
\@mathmargin=\skip49
For additional information on amsmath, use the `?' option.

(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/amsmath/amstext.st
y
Package: amstext 2000/06/29 v2.01 AMS text

(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/amsmath/amsgen.sty
File: amsgen.sty 1999/11/30 v2.0 generic functions
\@emptytoks=\toks15
\ex@=\dimen139
))
(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/amsmath/amsbsy.sty
Package: amsbsy 1999/11/29 v1.2d Bold Symbols
\pmbraise@=\dimen140
)
(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/amsmath/amsopn.sty
Package: amsopn 2016/03/08 v2.02 operator names
)
\inf@bad=\count187
LaTeX Info: Redefining \frac on input line 234.
\uproot@=\count188
\leftroot@=\count189
LaTeX Info: Redefining \overline on input line 399.
\classnum@=\count190
\DOTSCASE@=\count191
LaTeX Info: Redefining \ldots on input line 496.
LaTeX Info: Redefining \dots on input line 499.
LaTeX Info: Redefining \cdots on input line 620.
\Mathstrutbox@=\box47
\strutbox@=\box48
\big@size=\dimen141
LaTeX Font Info:    Redeclaring font encoding OML on input line 743.
LaTeX Font Info:    Redeclaring font encoding OMS on input line 744.
\macc@depth=\count192
\c@MaxMatrixCols=\count193
\dotsspace@=\muskip16
\c@parentequation=\count194
\dspbrk@lvl=\count195
\tag@help=\toks16
\row@=\count196
\column@=\count197
\maxfields@=\count198
\andhelp@=\toks17
\eqnshift@=\dimen142
\alignsep@=\dimen143
\tagshift@=\dimen144
\tagwidth@=\dimen145
\totwidth@=\dimen146
\lineht@=\dimen147
\@envbody=\toks18
\multlinegap=\skip50
\multlinetaggap=\skip51
\mathdisplay@stack=\toks19
LaTeX Info: Redefining \[ on input line 2923.
LaTeX Info: Redefining \] on input line 2924.
)
(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/generic/iftex/ifxetex.st
y
Package: ifxetex 2019/10/25 v0.7 ifxetex legacy package. Use iftex instead.
(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/generic/iftex/iftex.sty
Package: iftex 2020/03/06 v1.0d TeX engine tests
))
(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/generic/iftex/ifluatex.s
ty
Package: ifluatex 2019/10/25 v1.5 ifluatex legacy package. Use iftex instead.
) (c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/base/fontenc.sty
Package: fontenc 2020/08/10 v2.0s Standard LaTeX package
LaTeX Font Info:    Trying to load font information for T1+lmr on input line 11
2.
(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/lm/t1lmr.fd
File: t1lmr.fd 2009/10/30 v1.6 Font defs for Latin Modern
)) (c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/base/inputenc.s
ty
Package: inputenc 2020/08/01 v1.3d Input encoding file
\inpenc@prehook=\toks20
\inpenc@posthook=\toks21
) (c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/base/textcomp.st
y
Package: textcomp 2020/02/02 v2.0n Standard LaTeX package
)
(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/amsfonts/amssymb.s
ty
Package: amssymb 2013/01/14 v3.01 AMS font symbols

(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/amsfonts/amsfonts.
sty
Package: amsfonts 2013/01/14 v3.01 Basic AMSFonts support
\symAMSa=\mathgroup4
\symAMSb=\mathgroup5
LaTeX Font Info:    Redeclaring math symbol \hbar on input line 98.
LaTeX Font Info:    Overwriting math alphabet `\mathfrak' in version `bold'
(Font)                  U/euf/m/n --> U/euf/b/n on input line 106.
)) (c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/xcolor/xcolor.s
ty
Package: xcolor 2016/05/11 v2.12 LaTeX color extensions (UK)

(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/graphics-cfg/color
.cfg
File: color.cfg 2016/01/02 v1.6 sample color configuration
)
Package xcolor Info: Driver file: pdftex.def on input line 225.

(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/graphics-def/pdfte
x.def
File: pdftex.def 2020/10/05 v1.2a Graphics/color driver for pdftex
)
Package xcolor Info: Model `cmy' substituted by `cmy0' on input line 1348.
Package xcolor Info: Model `hsb' substituted by `rgb' on input line 1352.
Package xcolor Info: Model `RGB' extended on input line 1364.
Package xcolor Info: Model `HTML' substituted by `rgb' on input line 1366.
Package xcolor Info: Model `Hsb' substituted by `hsb' on input line 1367.
Package xcolor Info: Model `tHsb' substituted by `hsb' on input line 1368.
Package xcolor Info: Model `HSB' substituted by `hsb' on input line 1369.
Package xcolor Info: Model `Gray' substituted by `gray' on input line 1370.
Package xcolor Info: Model `wave' substituted by `hsb' on input line 1371.
)
(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/hyperref/hyperref.
sty
Package: hyperref 2021-02-27 v7.00k Hypertext links for LaTeX

(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/generic/ltxcmds/ltxcmds.
sty
Package: ltxcmds 2020-05-10 v1.25 LaTeX kernel commands for general use (HO)
)
(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/generic/pdftexcmds/pdfte
xcmds.sty
Package: pdftexcmds 2020-06-27 v0.33 Utility functions of pdfTeX for LuaTeX (HO
)

(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/generic/infwarerr/infwar
err.sty
Package: infwarerr 2019/12/03 v1.5 Providing info/warning/error messages (HO)
)
Package pdftexcmds Info: \pdf@primitive is available.
Package pdftexcmds Info: \pdf@ifprimitive is available.
Package pdftexcmds Info: \pdfdraftmode found.
)
(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/graphics/keyval.st
y
Package: keyval 2014/10/28 v1.15 key=value parser (DPC)
\KV@toks@=\toks22
)
(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/generic/kvsetkeys/kvsetk
eys.sty
Package: kvsetkeys 2019/12/15 v1.18 Key value parser (HO)
)
(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/generic/kvdefinekeys/kvd
efinekeys.sty
Package: kvdefinekeys 2019-12-19 v1.6 Define keys (HO)
)
(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/generic/pdfescape/pdfesc
ape.sty
Package: pdfescape 2019/12/09 v1.15 Implements pdfTeX's escape features (HO)
)
(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/hycolor/hycolor.st
y
Package: hycolor 2020-01-27 v1.10 Color options for hyperref/bookmark (HO)
)
(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/letltxmacro/letltx
macro.sty
Package: letltxmacro 2019/12/03 v1.6 Let assignment for LaTeX macros (HO)
)
(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/auxhook/auxhook.st
y
Package: auxhook 2019-12-17 v1.6 Hooks for auxiliary files (HO)
)
(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/kvoptions/kvoption
s.sty
Package: kvoptions 2020-10-07 v3.14 Key value format for package options (HO)
)
\@linkdim=\dimen148
\Hy@linkcounter=\count199
\Hy@pagecounter=\count266

(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/hyperref/pd1enc.de
f
File: pd1enc.def 2021-02-27 v7.00k Hyperref: PDFDocEncoding definition (HO)
Now handling font encoding PD1 ...
... no UTF-8 mapping file for font encoding PD1
)
(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/hyperref/hyperref-
langpatches.def
File: hyperref-langpatches.def 2021-02-27 v7.00k Hyperref: patches for babel la
nguages
)
(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/generic/intcalc/intcalc.
sty
Package: intcalc 2019/12/15 v1.3 Expandable calculations with integers (HO)
)
(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/generic/etexcmds/etexcmd
s.sty
Package: etexcmds 2019/12/15 v1.7 Avoid name clashes with e-TeX commands (HO)
)
\Hy@SavedSpaceFactor=\count267

(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/hyperref/puenc.def
File: puenc.def 2021-02-27 v7.00k Hyperref: PDF Unicode definition (HO)
Now handling font encoding PU ...
... no UTF-8 mapping file for font encoding PU
)
Package hyperref Info: Option `unicode' set `true' on input line 4073.
Package hyperref Info: Hyper figures OFF on input line 4192.
Package hyperref Info: Link nesting OFF on input line 4197.
Package hyperref Info: Hyper index ON on input line 4200.
Package hyperref Info: Plain pages OFF on input line 4207.
Package hyperref Info: Backreferencing OFF on input line 4212.
Package hyperref Info: Implicit mode ON; LaTeX internals redefined.
Package hyperref Info: Bookmarks ON on input line 4445.
\c@Hy@tempcnt=\count268
(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/url/url.sty
\Urlmuskip=\muskip17
Package: url 2013/09/16  ver 3.4  Verb mode for urls, etc.
)
LaTeX Info: Redefining \url on input line 4804.
\XeTeXLinkMargin=\dimen149

(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/generic/bitset/bitset.st
y
Package: bitset 2019/12/09 v1.3 Handle bit-vector datatype (HO)

(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/generic/bigintcalc/bigin
tcalc.sty
Package: bigintcalc 2019/12/15 v1.5 Expandable calculations on big integers (HO
)
))
\Fld@menulength=\count269
\Field@Width=\dimen150
\Fld@charsize=\dimen151
Package hyperref Info: Hyper figures OFF on input line 6075.
Package hyperref Info: Link nesting OFF on input line 6080.
Package hyperref Info: Hyper index ON on input line 6083.
Package hyperref Info: backreferencing OFF on input line 6090.
Package hyperref Info: Link coloring OFF on input line 6095.
Package hyperref Info: Link coloring with OCG OFF on input line 6100.
Package hyperref Info: PDF/A mode OFF on input line 6105.
LaTeX Info: Redefining \ref on input line 6145.
LaTeX Info: Redefining \pageref on input line 6149.

(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/base/atbegshi-ltx.
sty
Package: atbegshi-ltx 2020/08/17 v1.0a Emulation of the original atbegshi packa
ge
with kernel methods
)
\Hy@abspage=\count270
\c@Item=\count271
\c@Hfootnote=\count272
)
Package hyperref Info: Driver (autodetected): hpdftex.

(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/hyperref/hpdftex.d
ef
File: hpdftex.def 2021-02-27 v7.00k Hyperref driver for pdfTeX

(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/base/atveryend-ltx
.sty
Package: atveryend-ltx 2020/08/19 v1.0a Emulation of the original atvery packag
e
with kernel methods
)
\Fld@listcount=\count273
\c@bookmark@seq@number=\count274

(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/rerunfilecheck/rer
unfilecheck.sty
Package: rerunfilecheck 2019/12/05 v1.9 Rerun checks for auxiliary files (HO)

(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/generic/uniquecounter/un
iquecounter.sty
Package: uniquecounter 2019/12/15 v1.4 Provide unlimited unique counter (HO)
)
Package uniquecounter Info: New unique counter `rerunfilecheck' on input line 2
86.
)
\Hy@SectionHShift=\skip52
)
(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/geometry/geometry.
sty
Package: geometry 2020/01/02 v5.9 Page Geometry

(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/generic/iftex/ifvtex.sty
Package: ifvtex 2019/10/25 v1.7 ifvtex legacy package. Use iftex instead.
)
\Gm@cnth=\count275
\Gm@cntv=\count276
\c@Gm@tempcnt=\count277
\Gm@bindingoffset=\dimen152
\Gm@wd@mp=\dimen153
\Gm@odd@mp=\dimen154
\Gm@even@mp=\dimen155
\Gm@layoutwidth=\dimen156
\Gm@layoutheight=\dimen157
\Gm@layouthoffset=\dimen158
\Gm@layoutvoffset=\dimen159
\Gm@dimlist=\toks23
)
(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/fancyvrb/fancyvrb.
sty
Package: fancyvrb 2021/01/20 v3.7 verbatim text (tvz,hv)
\FV@CodeLineNo=\count278
\FV@InFile=\read2
\FV@TabBox=\box49
\c@FancyVerbLine=\count279
\FV@StepNumber=\count280
\FV@OutFile=\write3
) (c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/framed/framed.st
y
Package: framed 2011/10/22 v 0.96: framed or shaded text with page breaks
\OuterFrameSep=\skip53
\fb@frw=\dimen160
\fb@frh=\dimen161
\FrameRule=\dimen162
\FrameSep=\dimen163
)
(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/graphics/graphicx.
sty
Package: graphicx 2020/09/09 v1.2b Enhanced LaTeX Graphics (DPC,SPQR)

(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/graphics/graphics.
sty
Package: graphics 2020/08/30 v1.4c Standard LaTeX Graphics (DPC,SPQR)
(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/graphics/trig.sty
Package: trig 2016/01/03 v1.10 sin cos tan (DPC)
)
(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/graphics-cfg/graph
ics.cfg
File: graphics.cfg 2016/06/04 v1.11 sample graphics configuration
)
Package graphics Info: Driver file: pdftex.def on input line 105.
)
\Gin@req@height=\dimen164
\Gin@req@width=\dimen165
)
(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/l3backend/l3backen
d-pdftex.def
File: l3backend-pdftex.def 2021-03-02 L3 backend support: PDF output (pdfTeX)
\l__color_backend_stack_int=\count281
\l__pdf_internal_box=\box50
)
No file Mar-03-Homework.aux.
\openout1 = `Mar-03-Homework.aux'.

LaTeX Font Info:    Checking defaults for OML/cmm/m/it on input line 111.
LaTeX Font Info:    ... okay on input line 111.
LaTeX Font Info:    Checking defaults for OMS/cmsy/m/n on input line 111.
LaTeX Font Info:    ... okay on input line 111.
LaTeX Font Info:    Checking defaults for OT1/cmr/m/n on input line 111.
LaTeX Font Info:    ... okay on input line 111.
LaTeX Font Info:    Checking defaults for T1/cmr/m/n on input line 111.
LaTeX Font Info:    ... okay on input line 111.
LaTeX Font Info:    Checking defaults for TS1/cmr/m/n on input line 111.
LaTeX Font Info:    ... okay on input line 111.
LaTeX Font Info:    Checking defaults for OMX/cmex/m/n on input line 111.
LaTeX Font Info:    ... okay on input line 111.
LaTeX Font Info:    Checking defaults for U/cmr/m/n on input line 111.
LaTeX Font Info:    ... okay on input line 111.
LaTeX Font Info:    Checking defaults for PD1/pdf/m/n on input line 111.
LaTeX Font Info:    ... okay on input line 111.
LaTeX Font Info:    Checking defaults for PU/pdf/m/n on input line 111.
LaTeX Font Info:    ... okay on input line 111.

(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/epstopdf-pkg/epsto
pdf-base.sty
Package: epstopdf-base 2020-01-24 v2.11 Base part for package epstopdf
Package epstopdf-base Info: Redefining graphics rule for `.eps' on input line 4
85.

(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/latexconfig/epstop
df-sys.cfg
File: epstopdf-sys.cfg 2010/07/13 v1.3 Configuration of (r)epstopdf for TeX Liv
e
))
Package hyperref Info: Link coloring OFF on input line 111.

(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/hyperref/nameref.s
ty
Package: nameref 2021-04-02 v2.47 Cross-referencing by name of section

(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/refcount/refcount.
sty
Package: refcount 2019/12/15 v3.6 Data extraction from label references (HO)
)
(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/generic/gettitlestring/g
ettitlestring.sty
Package: gettitlestring 2019/12/15 v1.6 Cleanup title references (HO)
)
\c@section@level=\count282
)
LaTeX Info: Redefining \ref on input line 111.
LaTeX Info: Redefining \pageref on input line 111.
LaTeX Info: Redefining \nameref on input line 111.
\@outlinefile=\write4
\openout4 = `Mar-03-Homework.out'.

*geometry* driver: auto-detecting
*geometry* detected driver: pdftex
*geometry* verbose mode - [ preamble ] result:
* driver: pdftex
* paper: <default>
* layout: <same size as paper>
* layoutoffset:(h,v)=(0.0pt,0.0pt)
* modes: 
* h-part:(L,W,R)=(72.26999pt, 469.75502pt, 72.26999pt)
* v-part:(T,H,B)=(72.26999pt, 650.43001pt, 72.26999pt)
* \paperwidth=614.295pt
* \paperheight=794.96999pt
* \textwidth=469.75502pt
* \textheight=650.43001pt
* \oddsidemargin=0.0pt
* \evensidemargin=0.0pt
* \topmargin=-37.0pt
* \headheight=12.0pt
* \headsep=25.0pt
* \topskip=10.0pt
* \footskip=30.0pt
* \marginparwidth=65.0pt
* \marginparsep=11.0pt
* \columnsep=10.0pt
* \skip\footins=9.0pt plus 4.0pt minus 2.0pt
* \hoffset=0.0pt
* \voffset=0.0pt
* \mag=1000
* \@twocolumnfalse
* \@twosidefalse
* \@mparswitchfalse
* \@reversemarginfalse
* (1in=72.27pt=25.4mm, 1cm=28.453pt)

LaTeX Font Info:    Trying to load font information for OT1+lmr on input line 1
13.
(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/lm/ot1lmr.fd
File: ot1lmr.fd 2009/10/30 v1.6 Font defs for Latin Modern
)
LaTeX Font Info:    Trying to load font information for OML+lmm on input line 1
13.
(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/lm/omllmm.fd
File: omllmm.fd 2009/10/30 v1.6 Font defs for Latin Modern
)
LaTeX Font Info:    Trying to load font information for OMS+lmsy on input line 
113.
(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/lm/omslmsy.fd
File: omslmsy.fd 2009/10/30 v1.6 Font defs for Latin Modern
)
LaTeX Font Info:    Trying to load font information for OMX+lmex on input line 
113.
(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/lm/omxlmex.fd
File: omxlmex.fd 2009/10/30 v1.6 Font defs for Latin Modern
)
LaTeX Font Info:    External font `lmex10' loaded for size
(Font)              <12> on input line 113.
LaTeX Font Info:    External font `lmex10' loaded for size
(Font)              <8> on input line 113.
LaTeX Font Info:    External font `lmex10' loaded for size
(Font)              <6> on input line 113.
LaTeX Font Info:    Trying to load font information for U+msa on input line 113
.
(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/amsfonts/umsa.fd
File: umsa.fd 2013/01/14 v3.01 AMS symbols A
)
LaTeX Font Info:    Trying to load font information for U+msb on input line 113
.
(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/amsfonts/umsb.fd
File: umsb.fd 2013/01/14 v3.01 AMS symbols B
)
LaTeX Font Info:    Trying to load font information for T1+lmtt on input line 1
18.
(c:/Users/regen/AppData/Roaming/TinyTeX/texmf-dist/tex/latex/lm/t1lmtt.fd
File: t1lmtt.fd 2009/10/30 v1.6 Font defs for Latin Modern
)
LaTeX Font Info:    External font `lmex10' loaded for size
(Font)              <10> on input line 141.
LaTeX Font Info:    External font `lmex10' loaded for size
(Font)              <7> on input line 141.
LaTeX Font Info:    External font `lmex10' loaded for size
(Font)              <5> on input line 141.
! Missing $ inserted.
<inserted text> 
                $
l.180 ...t_0^1 3x(1-x)^2\ dx = 0.25\) \$Var = \int
                                                  \_0\^{}1 
Here is how much of TeX's memory you used:
 10352 strings out of 480971
 151563 string characters out of 5906255
 456987 words of memory out of 5000000
 27404 multiletter control sequences out of 15000+600000
 443037 words of font info for 64 fonts, out of 8000000 for 9000
 14 hyphenation exceptions out of 8191
 71i,6n,74p,249b,289s stack positions out of 5000i,500n,10000p,200000b,80000s

!  ==> Fatal error occurred, no output PDF file produced!


#--- 05-Feb-Homework.Rmd ---#
---
title: 'Homework #01'
author: "Robert Campbell"
date: "04 Feb 2021"
output:
  pdf_document: default
  html_document: default
---

```{r setup, echo=FALSE}
# This line makes your figures smaller (you want to do this!)
knitr::opts_chunk$set(fig.width=5, fig.height=3)

# This code loads the dplyr library
# You often want to load libraries at the start of your file
suppressWarnings(suppressMessages(library(dplyr)))
```

## Chapter 01

### Problem 01 
  a. x*2 = {2,4,6}
  b. x*y = {6,10,12}
  c. x[1]*y[2] = 5
  ```{r, echo=FALSE}
  x <- c(1,2,3)
  y <- c(6,5,4)
  x*2
  x*y
  x[1]*y[2]
  ```

### Problem 04
```{r, echo=FALSE}
p <- seq(from = 0, to = 1, by=0.2)
plot(p,p*(1-p),type='l', main="04 - B")
```
```{r, echo=FALSE}
p <- seq(from = 0, to = 1, by=0.01)
plot(p,p*(1-p),type='l', main="04 - C")

```

### Problem 05
```{r, echo=FALSE}
z <- seq(from=1,to=100,by=1)
z^2
```

### Problem 07
  b.
  ```{r, echo=FALSE}
  mean(rivers)
  sd(rivers)
  ```
  c.
  ```{r, echo=FALSE}
  hist(rivers)
  ```
  d.
  ```{r, echo=FALSE}
  summary(rivers)
  ```
  e.
  Shortest:
  ```{r, echo=FALSE}
  min(rivers)
  ```
  Longest:
  ```{r, echo=FALSE}
  max(rivers)
  ```
  f.
  ```{r, echo=FALSE}
  rivers[rivers>1000]
  ```

### Problem 08
  a. 153 observations of 6 variables
  b. {Ozone, Solar.R, Wind, Temp, Month, Day}
  c. {int, int, num, int, int, int}
  d. The datatypes make sense, worst case they could have been stored as strings or the num and int types could be switched. You would lose data, waste space, or lose functionality if they were changed.

### Problem 10
  a. 12
  b. Cars that displace more than 150 cu.ins. and get more than 20 miles per gallon
  c.
  ```{r, echo=FALSE}
  mtcars[mtcars$gear==4 & mtcars$am==1,]
  ```
  d.
  ```{r, echo=FALSE}
  mtcars[mtcars$gear==4 | mtcars$am==1,]
  ```
  e.
  ```{r, echo=FALSE}
  mean(mtcars[mtcars$carb==2,]$mpg)
  ```

### Problem 12
  ```{r, echo=FALSE}
  # import hotdogs
  hot_dogs<-fosdata::hot_dogs
  ```
  a. 3 variables: {Factor, int, int}
  b. {"Beef", "Meat", 'Poultry"}
  c. 645mg
  d. 
  ```{r, echo=FALSE}
  mean(hot_dogs[hot_dogs$type == "Beef",]$calories)
  ```
  
### Problem 13
```{r, echo=FALSE}
# Import DrinksWages
DrinksWages<- HistData::DrinksWages
```
  a.  70 observations of 6 variables of types:
  {Factor, Factor, int, int, num, int}
  b. A factory worker
  c. 
  ```{r, echo=FALSE}
  sum(DrinksWages$n)
  ```
  d. 
  ```{r, echo=FALSE}
  sum(DrinksWages$n * DrinksWages$wage)/sum(DrinksWages$n)
  ```

### Problem 15 a-e
```{r,echo=FALSE}
bechdel <- fosdata::bechdel
```
  a. 1794
  b. 991
  c.
  ```{r,echo=FALSE}
  table(bechdel$year)
  ```
  d. 2010
  e. 5


#--- 12-Feb-Homework.Rmd ---#
---
title: 'Homework #02'
author: "Robert Campbell"
date: "16 May 2021"
output:
  pdf_document: default
  html_document: default
---

```{r setup, echo=FALSE}
# This line makes your figures smaller (you want to do this!)
knitr::opts_chunk$set(fig.width=5, fig.height=3)

# This code loads the dplyr library
# You often want to load libraries at the start of your file
suppressWarnings(suppressMessages(library(dplyr)))
# Chapter 2 exercises # 1-5, 8-11, 17, 20, 21, 25, 26, 29
```

## Chapter 02   
### Problem 01      
  6/36 or 1/6
```{r}
mean(replicate(10000, {x<-sample(1:6, 2, replace=TRUE)
  ((x[1] == (2 * x[2])) || (x[2] == (2* x[1])))
  }))
```
   
### Problem 02      
  a. 6/36 or 1/6
```{r}
mean(replicate(10000, {x<-sample(1:6, 2, replace=TRUE)
  abs(x[1] - x[2]) == 0
}))
```   
  b. 4/36 or 1/9
```{r}
mean(replicate(10000, {x<-sample(1:6, 2, replace=TRUE)
abs(x[1] - x[2]) == 4
}))
```   
   
### Problem 03      
  a. P(not green) = 1 - .2 = .8
  b. P(r U o U y) = .13 + .2 + .14 = .47
  c. Approximately .2
```{r}
mm<- c("y","r","o","br","g","bl")
pb<-c(.14,.13,.2,.12,.2,.21)
mean(replicate(10000, {x<-sample(mm, 4, prob=pb, replace=TRUE);
  sum(x=="bl") > 1
}))
```   
  d. Approximately .01
```{r}
mean(replicate(10000, {x<-sample(mm, 6, prob=pb, replace=TRUE);
  ((sum(x=="y") == 1) & (sum(x=="r") == 1) & (sum(x=="o") == 1) &
    (sum(x=="br") == 1) & (sum(x=="g") == 1) & (sum(x=="bl") == 1)) 
}) )
```


### Problem 04      
  Approximatly .06   
```{r}
mean(replicate(10000, {x<-sample(mm, 30, prob=pb, replace=TRUE);
  ( (sum(x=="bl") > 8) & (sum(x=="o") > 5)) 
}) )
```   


### Problem 05      
Approximately 0.37 or 3/8   
```{r}
type<- c("o","a","b","ab")
pb<-c(.45,.40,.11,.04)

mean(replicate(10000, {x<-sample(type,2,prob=pb,replace=TRUE);
  x[1] == x[2]
}))
```
   

### Problem 08      
  Approximately 0.56
```{r}
mean(replicate(10000, {x<-sample(1:6, 5, replace=TRUE)
  ((14 < sum(x)) & (sum(x) < 21))
}))
```   


### Problem 09      
  Approximately 0.29
```{r}
mean(replicate(10000, {x<-sample(1:6, 20, replace=TRUE)
  y<-cumsum(x)
  sum(y == 20)
}))
```   


### Problem 10      
  a. 
```{r}
y <- replicate(10000, {x<-sample(1:6, 2, replace=TRUE)
sum(x)
})

hist(y, breaks=12)
```   
  b. 
```{r}
y <- replicate(10000, {x<-sample(c(2,6), replace=TRUE)
  x + 5
})

hist(y)
```   
  c. 
```{r}
y <- replicate(10000, {x<-sample(c(1,2,2,3,3,4), replace=TRUE)
  z<-sample(c(1,3,4,5,6,8), replace=TRUE)
  x + z
})

hist(y, breaks=12)

```   

### Problem 11      
  Assuming birth year is not accounted for, excluding Feb29
  Approximately 0.10
```{r}
mean(replicate(10000, {x<-sample(1:365, 200, replace=TRUE)
  sum(x==1) > 1
}))
```   
   

### Problem 17      
  Approximately 0.02
```{r}
s<-fosdata::scrabble

mean(replicate(10000, {x<-sample(s$piece,7,replace=FALSE)
  ((sum(x=='A')==0)&(sum(x=='E')==0)&(sum(x=='I')==0)&(sum(x=='O')==0)&(sum(x=='U')==0))
}))
```   


### Problem 20      
  a. C and D are disjoint, A and D are disjoint
  b. B and D are independent
  c. A and B, A and C, B and C


### Problem 21      
  a. A and C
  b. B and C
  c. 1


### Problem 25      
  a. 1/2
  b. p^2 * (1-p)^2 = p^2 * (1 - 2p + p^2) = p^3 - 2p^2 + p
  c.
```{r}
p<-seq(0,1,0.1)
y<- p^3 - 2*(p^2) + p
plot(p, y)
```      
  d. Prob same is approximately .51, different is approximately .49
```{r}
flip<-c("v", "c")
pb<-c(.45,(1-.45))

mean(replicate(10000, {x<-sample(flip,2, prob=pb,replace=TRUE)
  (x[1]==x[2])
}))
```   
  e. Choose same
  f. Yes, the two frisbee flip is closer to a .5 probability   


### Problem 26      
  Approximate .5
```{r}
people <- sample(0:1, 100000, prob=c(.99,.01), replace=TRUE)
test <- ifelse(people, 1, sample(0:1, 100000, prob=c(.99,.01), replace=TRUE))
1-(sum(test & !people)/sum(test))
```   


### Problem 29      
  a.   
```{r}
# P(B|A) = P(A|B) * (P(B)/P(A))
# If there is a second tree, there will always be a first tree
  1 * (.85/.91)
```   
  b. 





#--- Apr-12-Homework.Rmd ---#
---
title: 'Homework #08'
author: "Robert Campbell"
date: "12 Apr 2021"
output:
  pdf_document: default
  html_document: default
---

```{r setup, echo=FALSE, warning=FALSE, message=FALSE}
# This line makes your figures smaller (you want to do this!)
knitr::opts_chunk$set(fig.width=5, fig.height=3)

# This code loads the dplyr library
# You often want to load libraries at the start of your file
library(dplyr)
library(ggplot2)
#library(babynames)
library(tidyr)
#library(stringr)
library(ISwR)
library(Sleuth3)

# Homework Problems:
# Ch 8 Exercises # 14, 16, 17, 19, 25-32, 34, 35, 21, 23, 24
```


## Chapter 08
### Problem 14   

98% Confidence interval: [-1.037, -0.556]
P value is < 0.05, so the mean differs significantly from 0. This means that the two nurses' interpretations of the data differed significantly, effecting the values.
```{r}
react <- ISwR::react
xbar <- mean(react)
s <- sd(react)
stderror <- s/sqrt(length(react))
tcritical <- -qt(0.01,(length(react)-1))
xbar - tcritical * stderror  # lower
xbar + tcritical * stderror  # upper
t.test(react,conf.level = 0.98)
```

### Problem 16   
Hypothesis: Children's estimate of an object's weight will deviate from 0, meaning they will make inaccurate estimates.

The only accurate estimates were for the 200g object, all others were significantly different.
```{r}
child_est<- fosdata::weight_estimate %>% filter(age!= 'adult')
t.test(child_est$mean100, mu=100)
t.test(child_est$mean200, mu=200)
t.test(child_est$mean300, mu=300)
t.test(child_est$mean400, mu=400)
```

### Problem 17    
Population is Mexican-American adults in California, with a sample size of 102.
P-value = (1.743 x 10^-4) < 0.05, so the blood pressure significantly differs from the normal systolic blood pressure of 120 mm Hg.
```{r}
obesity<-ISwR::bp.obese
t.test(obesity$bp, mu=120)
```

### Problem 19
Natural null hypothesis is that the population's ratio of actual weight to ideal weight will not deviate from the mean of 1.

P-value is much less that 0.05, meaning that the population's mean ratio significantly differs from the null hypothesis.
```{r}
obesity<-ISwR::bp.obese
t.test(obesity$obese, mu=1)
```

### Problem 25   
  a. Pain felt by a laser shot on the hand will not significantly change based on the aesthetic quality of an observed painting.   
  b. There is no difference in appearance of objects between normal viewing and viewing by bending over and looking between your legs.   
  c. The time to urinate will differ based off of species.

### Problem 26   
  a. The mean of the sample is greater than the null hypothesis mean in all measurements.
  b. The quality of sleep did not see significant improvement. This can be seen by the P-value being greater than 0.05 (P = 0.27).   
  c. Reported sleep disturbance showed the most significance, having the smallest P-value of less than 0.01.

### Problem 27   
  P-value = 0.6463 > 0.05, there is no significant difference from between male and female blood pressure in the sample group.   
  It would not be appropriate to generalize this to the general American population. It is a single ethnic group, in a single geographic region, in a "small town", and does not account for the diversity in each of those larger categories that make up the general American population.

```{r}
obesity<-ISwR::bp.obese
t.test(bp ~ sex, data=obesity)
```

### Problem 28    
  There is significant evidence, with the p-value = 0.01308. With Fish Oil's mean reduction in diastolic blood pressure having a positive value while regular oil has a negative value showing that fish oil provides greater reduction in diastolic blood pressure on average. 
```{r}
oil<-Sleuth3::ex0112
t.test(BP ~ Diet, data=oil)
```

### Problem 29  
  No, the initial boxplot looks lopsided and the initial histograms look more exponential than normal.   
  After calculating the logs, the data does appear more normal.   
  The difference in mean of the log of brainsize between large and small litter size is not significant as P-value= 0.05225 is not less than 0.05, though it is close.

```{r}
size <- Sleuth3::ex0333
boxplot(BrainSize ~ LitterSize, data=size)
size %>% ggplot(aes(x=BrainSize)) + geom_histogram() + facet_grid(vars(LitterSize))
size<-mutate(size,logBrain=log(BrainSize))
boxplot(logBrain ~ LitterSize, data=size)
size %>% ggplot(aes(x=logBrain)) + geom_histogram() + facet_grid(vars(LitterSize))
t.test(logBrain ~ LitterSize,data=size)
```

### Problem 30   
  Hypothesis: There is a significant difference in volume in the affected brain regions of schizophrenic patients.
  The sample data is inconclusive with P-Value = 0.05691 being greater than 0.05, but close.
  
```{r, warning=FALSE, message=FALSE}
vol<-Sleuth3::case0202
pvol<-vol %>% pivot_longer(col=Unaffected:Affected, names_to = "Diagnosis", values_to = "BrainVolume")
pvol %>% ggplot(aes(x=BrainVolume)) + geom_histogram() + facet_grid(vars(Diagnosis))
t.test(BrainVolume ~ Diagnosis, pvol)
```

### Problem 31   
  a. The tk-12 provides a significantly different temperature change from the control with a P-value of (5.609 x 10^-15), and provides a cooling afffect as all data values are negative while the control are positive.
```{r}
moo<-fosdata::cows_small
t.test(moo$tk_12, moo$control, paired = TRUE)
```

  b. The tk-0.75 provides a significantly different temperature change from the control with a P-value of (3.747 x 10^-14), and provides a cooling effect as all data values are negative while the control are positive.
```{r}
t.test(moo$tk_0_75, moo$control, paired = TRUE)
```


### Problem 32   
  There is not a significant difference in task completion time based on gender as the P-value: 0.2033 much larger than the alpha: 0.01

```{r}
child<-fosdata::child_tasks
t.test(day_night_completion_time_secs ~ gender, data=child)
```

### Problem 21   
  a. The mean of 4 was within the confidence interval. (May not show that way after the knit)   
```{r}
x<-rnorm(10,mean=4, sd=1)
t.test(x,conf.level = 0.95)
```
  b. 
```{r}
z<-replicate(10000, {x<-rnorm(10,mean=4, sd=1);
  y<-t.test(x,conf.level = 0.95)$conf.int;
  (4 > y[1]) && (4<y[2])})
mean(z)
```

### Problem 23   
  a. The mean of 4 did appear in the confidence interval. (This may not match the R output after knitting)
```{r}
x<-rexp(10,0.25)
t.test(x,conf.level = 0.95)
```
  b. The result is not .95 because the data is heavily skewed left.
```{r}
z<-replicate(10000, {x<-rexp(10,0.25);
  y<-t.test(x,conf.level = 0.95)$conf.int;
  (4 > y[1]) && (4<y[2])})
mean(z)
```
  c. This is closer to .95 because of the larger sample in each experiment iteration, which brings the sample mean closer to the true mean.
```{r}
z<-replicate(10000, {x<-rexp(100,0.25);
  y<-t.test(x,conf.level = 0.95)$conf.int;
  (4 > y[1]) && (4<y[2])})
mean(z)
```

### Problem 24   
```{r}
pval<- replicate(10000,{x<-rnorm(20,0,1);
  y<-convolve(x, c(1, 1), type = "filter");
  t.test(y, mu=0)$p.value})
mean(pval>0.05)
```




#--- Apr-26-Homework.Rmd ---#
---
title: 'Homework #09'
author: "Robert Campbell"
date: "26 Apr 2021"
output:
  html_document: default
  pdf_document: default
---

```{r setup, echo=FALSE, warning=FALSE, message=FALSE}
# This line makes your figures smaller (you want to do this!)
knitr::opts_chunk$set(fig.width=5, fig.height=3)

# This code loads the dplyr library
# You often want to load libraries at the start of your file
library(dplyr)
#library(ggplot2)
#library(tidyr)
#library(stringr)


# Homework Problems:
# Ch 9 Exercises # 2, 4, 6, 9, 10, 11, 15, 16
```


## Chapter 09
### Problem 02   
```{r}
2*sum(dsignrank(601:1000,40))
```

### Problem 04   
```{r}
x <- c(-1,0,2,8)
mu <- 3
xdif <- x-mu
xrank<-rank(abs(xdif))
#sum(xrank[xdif > 0])
v<-0:10
plot(v,dsignrank(v,4))
2*(sum(dsignrank(0:4, 4)))

wilcox.test(x, mu=mu)
```

### Problem 06   

  a. The data seems reasonably symmetric.
```{r}
weight<- fosdata::weight_estimate$mean200
boxplot(weight)
```   
  b. The weight that was estimated by the population was not close to the actual weight of the object.
```{r}
wilcox.test(weight, mu=200)
```   

### Problem 09   
  a.
```{r, warning=FALSE, message=FALSE}
sparrow<-Sleuth3::ex0221
boxplot(sparrow$Humerus[sparrow$Status == "Perished"])
boxplot(sparrow$Humerus[sparrow$Status == "Survived"])
```   
  b. 
```{r, warning=FALSE, message=FALSE}
wilcox.test(Humerus ~ Status, data=sparrow)
```
   
  c. No, there is not enough evidence to conclude that there is significant difference in Humerus length between the two populations.

### Problem 10   
  a. Yes, there is significant difference in the height of the flowers.
```{r, message=FALSE, warning=FALSE}
flower<- Sleuth3::ex0428
wilcox.test(flower$Cross, flower$Self)
```
   
  b. Both datasets are skew with one significant outlier.
```{r}
boxplot(flower$Cross)
boxplot(flower$Self)
```
  
### Problem 11   
  a. Hypothesis: There will be significant difference in antibody levels between the sample with malaria symptoms and the sample without.   
    P = 2.127e-05, reject the null, there is significant difference in antibody levels.
```{r}
malaria <- ISwR::malaria
wilcox.test(ab ~ mal, data=malaria)
```
   
  b. No, the data is not normal and does not meet the assumptions of the t.test
```{r}
hist(malaria$ab[malaria$mal == 0])
hist(malaria$ab[malaria$mal == 1])
```
   
  c. Taking the log of the data does make it roughly normal, making the t.test more viable.
```{r}
hist(log(malaria$ab[malaria$mal == 0]))
hist(log(malaria$ab[malaria$mal == 1]))
```
   
  d. Like the Wilcoxon test, this does show significance with a p-value of 2.376e-05. However, the p-value from the t.test is larger than the Wilcoxon p-value 2.127e-05, the difference is likely due to the fact that even the log of the data is still not completely normal.
```{r}
t.test(log(ab) ~ mal, data=malaria)
```

### Problem 15   
  a. ~16% of the time
```{r}
test<-replicate(10000,{x<-rnorm(100,0.1,1); t.test(x, mu=0)$p.value})
mean(test<0.05)
```
   
  b. ~16% of the time
```{r}
test<-replicate(10000,{x<-rnorm(100,0.1,1); wilcox.test(x, mu=0)$p.value})
mean(test<0.05)
```
   
  c. t.test: ~88% of the time
```{r}
test<-replicate(10000,{x<-rnorm(1000,0.1,1); t.test(x, mu=0)$p.value})
mean(test<0.05)
```
   
     wilcox.test: ~88% of the time
```{r}
test<-replicate(10000,{x<-rnorm(1000,0.1,1); wilcox.test(x, mu=0)$p.value})
mean(test<0.05)
```
   
  d. Since their accuracy is similar, anecdotally I'd suggest running t.test as wilcox.test took significantly longer with larger n.  

### Problem 16   
  a.
    i. The true mean is 0
    ii. Type-II
  b. Continuing the assumptions from a, a Type-I error has 0% probability of occurring as it cannot happen when the null is false.
  c. Type-I error has 0% probability of occurring as it cannot happen when the null is false.
  d. Type-II error has ~1.2% probability of occurring in a t.test
```{r}
pvals <- replicate(10000, {
  x <- rt(20, df=86)
  t.test(x, mu=1)$p.value
})
power<- mean(pvals < 0.05)
1 - power
```
  e. Type-II error has ~1.7% probability of occurring in a wilcox.test
```{r}
pvals <- replicate(10000, {
  x <- rt(20, df=86)
  wilcox.test(x, mu=1)$p.value
})
power<- mean(pvals < 0.05)
1 - power
```
   
  f. It looks like t.test is more powerful when used on a t-distribution of data.



#--- exam-1.Rmd ---#
---
title: "Exam 1"
author: "Robert Campbell"
date: "Friday, March 5, 2021"
output: pdf_document
---

Place your answers into this markdown document, knit it, and hand in the result as a PDF.
There are 9 questions, each worth 10 points.  Additionally, you get 10 points for handing in a PDF of a knit markdown document.

You may use R, the internet, and any reference material,
but do not work together and do not get help (except from Dr. Clair).

### Honor Pledge
The work I have submitted represents my own effort. While working on this exam, I did not communicate in any form with individuals other than the instructor.

Signed: Robert V. Campbell

### Problem 1
Suppose you roll an ordinary six sided die 20 times and add up the total.
What is the probability the sum is 55 or less?
```{r}
x<-replicate(10000,sum(sample(1:6,20,replace=TRUE)))
mean(x<56)
```


### Problem 2
McDonald's happy meals currently contain a package of four Pokemon cards. There are 25 different cards, all equally likely.

a. If you buy one happy meal (4 cards), what is the probability you get a Pikachu card?
```{r}
x<-replicate(10000,sum(sample(1:25, 4, replace=TRUE) == 1))
mean(x>0)
```

b. If you buy 10 happy meals, what is the probability you get a Pikachu card?
```{r}
x<-replicate(10000,sum(sample(1:25, 40, replace=TRUE) == 1))
mean(x>0)
```
c. How many happy meals do you need to buy to have a 99% chance of getting a Pikachu?
```{r}
x<-replicate(10000,sum(sample(1:25, 116, replace=TRUE) == 1))
mean(x>0)
116/4
```


### Problem 3
Roll two dice and consider the events

- $A$: both dice are even
- $B$: the sum of the dice is 8

a. What is $P(A)$?
  $P(A) = \frac{9}{36} = \frac{1}{4}$
b. What is $P(A|B)$?
   $P(A|B) = \frac{P(A\&B)}{P(B)} = \frac{3/36}{5/36}=\frac{3}{5}$
c. Are $A$ and $B$ independent events?
  No.

### Problem 4

The random variable $X$ has the pdf shown here:

```{r echo=FALSE}
px <- seq(-1,3,.1)
plot(px,pmax(1-abs(px-1),0),type='l',xlab="X",ylab="")
```

a. What is $P(X < 1)$?
  $P(X < 1) = \frac{1}{2}$
b. What is $P(X > 1.5)$?
  $P(X > 1.5) = \frac{1}{8}$
c. What is $E[X]$?
  $E[X] = 1$

### Problem 5

A high voltage spark can be used to create tiny aerosol particles of gold.
These particles have diameters which vary according to a normal distribution with mean 1.35nm and standard deviation 0.24nm.

a. What proportion of these particles have diameter less than 1nm?
```{r}
x<-rnorm(10000,1.35,0.24)
mean(x<1)
```

b. What proportion of these particles have diameter larger than 2nm?
```{r}
mean(x>2)
```

### Problem 6
Back in 2006, the website MySpace leaked a large number of its user's passwords.
The passwords had this distribution:

- Numbers only: 1.3%
- Letters only: 9.5%
- Alphanumeric (both letters and numbers): 81%
- Includes symbols: 8.2%

a. What is the probability that a MySpace user had no Symbols in their password?
  $1-.082=  0.918$

b. If you chose 10 MySpace users at random, what is the probability that all of them had Alphanumeric passwords?

```{r}
x<- replicate(10000,sum(sample(c('N','L','AN','S'), 10, replace=TRUE,prob=c(.013,.095,.81,.082)) == 'AN'))
mean(x==10)
```
### Problem 7

Let $X$ and $Y$ be normal random variables with mean 10 and sd 5.

Find the expected distance from the point $(X,Y)$ to the origin $(0,0)$.
```{r}
sqrt((10^2)+(10^2))

```

### Problem 8

The `ChestSizes` data from the `HistData` library has measurements of Scottish Militiamen.
There are two variables: `chest`, which gives the chest size in inches, and `count` which gives the number of soldiers with that chest size.

a. Calculate the total number of soldiers by summing the `count` variable.
```{r}
sum(HistData::ChestSizes$count)

```

b. Calculate the mean chest size by summing (chest size)*(count)/(total number of soldiers).
```{r}
sum((HistData::ChestSizes$count)*(HistData::ChestSizes$chest))/sum(HistData::ChestSizes$count)
```

### Problem 9
Continue with `HistData::ChestSizes`.

a. Plot the distribution of chest sizes with `chest` on the $x$-axis and `count` on the $y$-axis.
```{r}
plot(HistData::ChestSizes$chest,HistData::ChestSizes$count)
```
b. What sort of random variable could model this distribution?
  Normal RV

c. From your plot, estimate (by eye) the mean and standard deviation of chest sizes.
  Mean: 40, sd: 2

#--- Feb-22-Homework.Rmd ---#
---
title: 'Homework #03'
author: "Robert Campbell"
date: "22 Feb 2021"
output:
  pdf_document: default
  html_document: default
---

```{r setup, echo=FALSE}
# This line makes your figures smaller (you want to do this!)
knitr::opts_chunk$set(fig.width=5, fig.height=3)

# This code loads the dplyr library
# You often want to load libraries at the start of your file
suppressWarnings(suppressMessages(library(dplyr)))
# Homework Problems:
# 1, 3, 5, 7, 9, 11, 14, 15, 16, 17, 19, 21, 23

```

## Chapter 03

### Problem 01
  a. p(x) is a valid probability mass function 
     $\frac{1}{4} + \frac{1}{2} + \frac{1}{8} + \frac{1}{8} = 1$
  b. $P(X\geq2) = \frac{1}{8} + \frac{1}{8} = \frac{1}{4}$
  c. $P(X\geq2 | X\geq1) =\frac{1}{2} + \frac{1}{8} + \frac{1}{8} = \frac{5}{8}$
  d. $P(X\geq2 \bigcup X\geq1) =\frac{1}{2} + \frac{1}{8} + \frac{1}{8} = \frac{5}{8}$
  
### Problem 03
  $1 = C(1+\frac{1}{2}+\frac{1}{4})$
  $\frac{1}{C} = \frac{7}{4}$
  $C = \frac{4}{7}$

### Problem 05
```{r}
X <- c(0,1,2,3)
prob <- c(1/4, 1/2, 1/8, 1/8)
mean(sample(X, 10000, prob, replace=TRUE))
```

### Problem 07
```{r}
d1 <- sample(c(1,2,3,4,5,6), 10000, replace=TRUE)
d2 <- sample(c(1,2,3,4,5,6), 10000, replace=TRUE)
mean(d1*d2)
```

### Problem 09
  0:999 has 1000 numbers with 10 possible 2 digit numbers(0:9), 90 possible 3 digit numbers(10:99), and 900 3 digit numbers(100:999).
  ```{r}
  mean(sample(c(1,2,3), 10000, prob=c(1/1000, 90/1000, 900/1000), replace=TRUE))
  ```

### Problem 11
  Bernoulli trial, red being a win, with p = 18/38
  a. Expected value is np, so the expected payout would be \[\$1 * \frac{18}{38} = \$0.47\]
  b. $P(A|B)=\frac{P(A\&B)}{P(B)}= \frac{\frac{18}{38}*\frac{18}{38}}{\frac{18}{38}}=\frac{18}{38}$
     The expected payout would be $\$2 * \frac{18}{38} = \$0.97$

### Problem 14
Note: According to the website's probabilities, you can win a front pair and a straight so I am treating the "don't care" portions of the probabilities ignoring the better results. The other option would be to change front and back pair to 9/1000 instead of 1/100. This same logic will carry throughout the problem.
  a. $\frac{1}{10} * \frac{1}{10} * \frac{1}{10} = \frac{1}{1000}$
  b. $\frac{1}{10} * \frac{1}{10} * \frac{10}{10} = \frac{1}{100}$
  c. $\frac{10}{10} * \frac{1}{10} * \frac{1}{10} = \frac{1}{100}$
  d. $\frac{3}{10} * \frac{2}{10} * \frac{1}{10} = \frac{6}{1000}$
  e. 
  f. 

### Problem 15
$E[X] = \sum_1^k(X*p(x)) = \frac{k(k+1)}{2}$

### Problem 16
  a. 
  ```{r}
  test <- rbinom(10000, 20, .25)
  (mean(test)/20)*100
  ```
  b. 
  ```{r}
  (sum(test>=10)/10000)
  ```
### Problem 17
  a. 
  ```{r}
  x <- rbinom(10000,10, .91)
  mean(x)
  ```
  b.
  ```{r}
  (sum(x >= 8)/10000)
  ```
  
### Problem 19
X <- rbinom(trials, 200, .55)
plot(table(X))
mean(X)
var(X)
# sd is sqrt(np(1-p))
sd(X)
# margin of error
2*sd(X)/200
X < 100 # This is the event that prop A looks like it will fail

  a. Expected number of votes:
  ```{r}
  x <- rbinom(10000, 200, .55)
  mean(x)
  ```
  b. Margin of error in percent of votes:
  ```{r}
  (2*sd(x))/200 * 100
  ```
  c. Probability prop A will fail?
  ```{r}
  (sum(x<100)/10000)
  ```
  d.
  $2\% = \frac{2*\sqrt{n(.55)(1-.55)}}{n}$
  $(.01n)^2 = n(.55)(.45)$
  $n = $

### Problem 21
  a. $P(y): 1/4, y = -1; 1/2, y = 0; 1/8, y =1; 1/8, y=2$
  b. $P(u): 1/4, u = 0; 1/2, u = 1; 1/8, u=4; 1/8, u = 9$
  c. $P(v): 1/2, v=0; 3/8, v = 1; 1/8, v = 4$

### Problem 23
```{r}
X <- c(0,1,2,3)
prob <- c(1/4, 1/2, 1/8, 1/8)
y <- sample(X, 10000, prob, replace=TRUE)
```
  Variance:
  ```{r}
  var(y)
  ```
  Standard Deviation:
  ```{r}
  sd(y)
  ```

#--- homework-template.Rmd ---#
---
title: "Homework #xx"
author: "Student"
date: '`r format(Sys.Date(), "%B %d, %Y")`'
output: html_document
---

```{r setup, echo=FALSE}
# This line makes your figures smaller (you want to do this!)
knitr::opts_chunk$set(fig.width=5, fig.height=3)

# This code loads the dplyr library
# You often want to load libraries at the start of your file
suppressWarnings(suppressMessages(library(dplyr)))
```

## Chapter xx

### Problem 1
My first homework problem

### Problem 2
My second homework problem


#--- Mar-03-Homework.Rmd ---#
---
title: 'Homework #04'
author: "Robert Campbell"
date: "03 Mar 2021"
output:
  html_document: default
  pdf_document: default
---

```{r setup, echo=FALSE}
# This line makes your figures smaller (you want to do this!)
knitr::opts_chunk$set(fig.width=5, fig.height=3)

# This code loads the dplyr library
# You often want to load libraries at the start of your file
suppressWarnings(suppressMessages(library(dplyr)))
# Homework Problems:
# Ch 3 Exercises # 18, 30, 31
# Ch 4 Exercises # 1,2,6,7,8,11,12,15,17,19,20,21,22

```

## Chapter 03
```{r}
trials <- 100000
```
### Problem 18
```{r}
x <- rgeom(trials,.09)
y <- x + 1
mean(y==20)
```

### Problem 30
  $\frac{Var(eN)}{E[eN]} = \frac{e^2*Var(N)}{e*E[N]} = \frac{eN}{N} = e$

### Problem 31
****Requires infinite of e^x
  1 + x + x^2 / 2!  + x^3 / 3! + ...
  


## Chapter 04

### Problem 01
 a. $P(X \geq0.5)=\int_.5^1 2x\ dx = \frac{1^2 * 2}{2} - \frac{0.5^2 * 2}{2} = 1 - .25 = .75$
 b. $P(X\geq 0.5 | X\geq0.25) = \frac{P(X\geq 0.5 \& X\geq0.25)}{P(X\geq0.25)}$
    $\frac{\int_.5^1 2x\ dx}{\int_.25^1 2x\ dx} = \frac{0.75}{1 - 0.25^2} = \frac{.75}{0.9375} = 0.8$

### Problem 02
  $1 = \int_0^1 Cx^2\ dx + \int_1^2C(2-x)^2\ dx = C*[\int_0^1x^2\ dx + \int_1^2(2-x)^2\ dx]$
  $1 = C*[\frac{1}{3} + \frac{1}{3}]$
  $C = \frac{3}{2}$

### Problem 06
  a. $1 = \int_0^13*(1-x)^2\ dx = 3 * -\int_1^0u^2\ du = 3 * [\frac{1}{3} - \frac{0}{3}] = 1$
  b. $Mean = \int_0^1 3x(1-x)^2\ dx = 0.25$
     $Var = \int_0^1 3x^2(1-x)^2\ dx - Mean^2 = 0.1 - 0.0625 = 0.0375 $
  c. $P(x \leq 0.5) = \int_0^.5 3*(1-x)^2\ dx = 0.875$
  d. $P(x\leq 0.5 | x \geq 0.25)= \frac{P(x\leq 0.5\ \&\ x \geq 0.25)}{P(x\geq0.25)} = \frac{\int_.25^.5 3*(1-x)^2\ dx}{\int_.25^1 3*(1-x)^2\ dx}$
     $\frac{\frac{19}{64}}{\frac{27}{64}} = \frac{19}{27} = 0.703704$

### Problem 07
  $Var(x) = 3$
  $Var(2x+1) = 2^2 * Var(X) + Var(1) = 4 * 3 + 0 = 12$
  
### Problem 08
  a. Estimate mean: 1
  b. Estimate sd: 1
  c. a = 0
  d. Lower bound estimate $P(0 <= x <=2): 0.225 * 2 = 0.45 $
### Problem 11
  a. 
```{r}
1 - pnorm(85, 80, 5)
```
  b.
```{r}
  mean(replicate(trials, sum(rnorm(10,80,5)>=85))>3)
```

### Problem 12
  a.
```{r}
hist(rnorm(trials, 5036, 122))
```
  b. 0, the probability of a specific number occuring in a continuous probability function is 0. The question could be modified to ask "what is the probability of the rope breaking at more than 5000lbs, but less than 5001lbs, which could be calculated as:
```{r}
pnorm(5001, 5036,122) - pnorm(5000, 5036, 122)
```

  c.
```{r}
qnorm(.95,5036,122)
```

### Problem 15
```{r}
x <- seq(0,1,.1)
plot(x, dunif(x,0,1),type='l') 
plot(x, punif(x,0,1),type='l') 
```

### Problem 17
```{r}
x <- rexp(trials,0.25)
```
  a.
```{r}
mean(x)
```
  b. By inspection, it would be a = 0 and does not contain the mean.

### Problem 19
```{r}
c1<- rexp(trials, 1/5)
c2<- rexp(trials)
mean(pmax(c1,c2)<10)

```

### Problem 20
See attached handwritten work.

### Problem 21
  a. It is called the memoryless property because previous events do not affect the probability of the next event.
  b. $P(X\geq a)\int_a^\infty \lambda e^{-\lambda x}\ dx = \frac{-\lambda e^{-\lambda x}}{\lambda}|_a^\infty$
     $0 - (-e^{-a\lambda}) = e^{-a\lambda}$
  c. 

### Problem 22
  a. Binomial
  P(Y==3):
```{r}
dbinom(3,10,1/6)
dice<-rbinom(trials,10,1/6)
mean(dice)
var(dice)
``` 
  b. Poisson, in accidents per week
```{r}
dpois(2,2)
traf<-rpois(trials,2)
mean(traf)
var(traf)
```
  c. Uniform, $Mean = \frac{60-0}{2} = 30$
  d. Exponential with rate in customers per hour 
```{r}
cust <- rexp(trials,5)
mean(cust)
mean(cust<(10/60))
```
  e. Geometric
```{r}
coin <- rgeom(trials,.5)
mean(coin)
mean(coin<4)
```
  f. Normal, mean of 98.6F, var of $100F - 98.6F = 1.4F$

#--- Mar-24-Homework.Rmd ---#
---
title: 'Homework #06'
author: "Robert Campbell"
date: "24 Mar 2021"
output:
  pdf_document: default
  html_document: default
---

```{r setup, echo=FALSE}
# This line makes your figures smaller (you want to do this!)
knitr::opts_chunk$set(fig.width=5, fig.height=3)

# This code loads the dplyr library
# You often want to load libraries at the start of your file
suppressWarnings(suppressMessages(library(dplyr)))
suppressWarnings(suppressMessages(library(ggplot2)))
suppressWarnings(suppressMessages(library(babynames)))
suppressWarnings(suppressMessages(library(tidyr)))
suppressWarnings(suppressMessages(library(stringr)))
# Homework Problems:
# Ch 6 Exercises # 1,4,5,8,9,13,15,19-23,26,28
```


## Chapter 06
### Problem 01
```{r}
quakes %>% ggplot(aes(x=lat, y=long, color=depth, size=mag)) + geom_point()
```

### Problem 04
```{r}
fosdata::brake %>% ggplot(aes(x=p1_p2)) + geom_density()
```
  a. The data is skewed
  b. The most likely time appears to be about 1125ms
  
### Problem 05
```{r}
fosdata::brake %>% ggplot(aes(sample=latency_p1)) + geom_qq() + geom_qq_line()
```
The data appears to be normal, but right skewed

### Problem 08
```{r}
fosdata::austen %>% filter(novel=="Pride and Prejudice") %>% group_by(chapter) %>% 
  summarize(sentiment = mean(sentiment_score)) %>%
  ggplot(aes(x=sentiment, y=chapter, color=chapter)) + geom_col()
```

### Problem 09
```{r}
suppressWarnings(suppressMessages(ISwR::juul %>% ggplot(aes(x=age, y=igf1,color=tanner)) +
                   geom_point() + geom_line() + geom_smooth()))
```
  c. Smooth seems to fit as the data does seem to more or less follow the trendline
     Line does not seem appropriate as the data jumps around that trendline in clusters

### Problem 13
```{r}
suppressWarnings(suppressMessages(fosdata::pres_election %>% mutate(party=factor(party)) %>%
  group_by(party, year) %>% summarize(votes=sum(candidatevotes,na.rm=TRUE)) %>%
  ggplot(aes(x=party, y=votes,fill=party)) + geom_col() + facet_wrap(vars(year))))
```

### Problem 15
  a.
```{r}
Lahman::Batting %>% group_by(yearID) %>% ggplot(aes(x=yearID, y=X2B, color=yearID)) + geom_point()
```
  b.
```{r}
suppressWarnings(suppressMessages(Lahman::Batting %>% group_by(yearID,lgID) %>%
  filter(lgID == c("AL", "NL")) %>% ggplot(aes(x=yearID, y=X2B, color=lgID)) +
    geom_point() + scale_color_manual(values = c("red1","blue"))))
```

### Problem 19
```{r}
suppressWarnings(suppressMessages(babynames %>% group_by(year, sex) %>% summarize(births = sum(n)) %>% 
  ggplot(aes(x=year, y=births, color=sex)) + geom_line()))
```
### Problem 20
```{r}
suppressWarnings(suppressMessages(babynames %>% group_by(year, sex) %>%
  summarize(u_names = n()) %>% ggplot(aes(x=year, y=u_names, color=sex)) +
    geom_line()))
```

### Problem 21
```{r}
babynames %>% filter(name %in% "Robert", sex=="M") %>%
  ggplot(aes(x=year,y=n)) + geom_line()
```

### Problem 22
```{r}
suppressWarnings(suppressMessages(babynames %>% filter(name %in% c("Bryan", "Brian"), sex=="M") %>%
  ggplot(aes(x=year,y=n, color=name)) + geom_line()))
```

### Problem 23
```{r}
suppressWarnings(suppressMessages(babynames %>% filter(name %in% "Jessie") %>%
  ggplot(aes(x=year,y=n, color=sex)) + geom_line()))
```
  - More male than female (~1950-1980)
  - More female than male (prior to 1950)
  - About the same (1980 and later)
  
  
### Problem 26
  a.
```{r, echo=FALSE}
tidy_data<-fosdata::scotland_births %>% pivot_longer(!age, names_to="Year", values_to="Births")
tidy_data$Year<-str_remove(tidy_data$Year, "x")
tidy_data$Year<-as.integer(tidy_data$Year)
tidy_data %>% group_by(age,Year) %>% ggplot(aes(x=Year,y=Births, group=age)) + geom_line()
```
  b.
```{r}
tidy_20<-tidy_data %>% filter(age == 20)
tidy_30<-tidy_data %>% filter(age==30)
tidy_data %>% group_by(age,Year) %>% ggplot(aes(x=Year,y=Births, group=age)) +
  geom_line() +geom_line(data=tidy_20, aes(x=Year,y=Births),color='red')+
geom_line(data=tidy_30, aes(x=Year,y=Births),color='blue') + labs(title="Scotland Births Over Time by Mother's Age \nRed: Age 20 \tBlue: Age 30")
```
### Problem 28



#--- Mar-31-Homework.Rmd ---#
---
title: 'Homework #07'
author: "Robert Campbell"
date: "31 Mar 2021"
output:
  pdf_document: default
  html_document: default
---

```{r setup, echo=FALSE, warning=FALSE, message=FALSE}
# This line makes your figures smaller (you want to do this!)
knitr::opts_chunk$set(fig.width=5, fig.height=3)

# This code loads the dplyr library
# You often want to load libraries at the start of your file
library(dplyr)
#library(ggplot2)
#library(babynames)
#library(tidyr)
#library(stringr)

# Homework Problems:
# Ch 5 Exercises # 25, 26, 28, 30, 34
# Ch 8 Exercises # 1, 3, 4, 6, 7, 11, 13
```


## Chapter 05
### Problem 25  
N = 8
```{r}
n<-8
trials<-10000
mu<-.5*(0+1)
sigma<- sqrt((1/12)*(1-0)^2)
sim<-replicate(trials, {x<-runif(n,0,1);
(mean(x)-mu) / (sigma/sqrt(n))
})
plot(density(sim))
curve(dnorm(x),add=TRUE,col="red")
```

### Problem 26  
By inspection, mu = 10, sigma = 2.5
```{r}
mu<-10
sigma<-2.5
n<-100
sumNorm<-replicate(10000,{x <- replicate(n, x<-sum(rexp(20, 2)));
                  (mean(x)-mu)/(sigma/sqrt(n))})
plot(density(sumNorm))
```

### Problem 28  
N = 36
```{r}
n<-36
trials<-10000
k<-2
mu<-k
sigma<- sqrt(2*k)
sim<-replicate(trials, {x<-rchisq(n,df=k);
(mean(x)-mu) / (sd(x)/sqrt(n))
})
plot(density(sim))
curve(dnorm(x),add=TRUE,col="red")
```

### Problem 30  
#### a.  

```{r}
curve(dt(x, 1), from = -5, to = 5)
```

#### b.  
```{r}
x<-replicate(10,mean(rt(100,1)))
y<-replicate(10,mean(rt(1000,1)))
z<-replicate(10,mean(rt(10000,1)))
x;y;z
```

#### c.  

```{r}
plot(density(x))
plot(density(y))
plot(density(z))
```

### Problem 34  
```{r}
n<-8
mu<-2
sim<-replicate(10000, {x<-rnorm(n,2,3);
(mean(x)-mu) / (sd(x)/sqrt(n))
})
plot(density(sim))
curve(dt(x,df=7),add=TRUE,col="red")
```


## Chapter 08
### Problem 01  
```{r}
n<-12
mu<-1
sigma<-3
sim<-replicate(10000, {x<-rnorm(n,1,sigma);
(mean(x)-mu) / (sigma/sqrt(n))
})
plot(density(sim))
curve(dt(x,df=11),add=TRUE,col="red")
```

### Problem 03  
C = 1.439756
```{r}
qt(0.1, df=6)
```

### Problem 04  
The smaller the degrees of freedom, the more area in the tail, the greater the variance.
```{r}
1-pnorm(2)
1-pt(2,df=40)
1-pt(2,df=20)
1-pt(2,df=10)
1-pt(2,df=5)

curve(dnorm(x), from = -5, to = 5, lwd=2)
curve(dt(x,df=40),add=TRUE,col="red")
curve(dt(x,df=20),add=TRUE,col="blue")
curve(dt(x,df=10),add=TRUE,col="green")
curve(dt(x,df=5),add=TRUE,col="yellow")
```

### Problem 06  
99% confidence interval [16.49394,20.0219]
```{r}
plastics <- fosdata::plastics %>% filter(!is.na(diameter))
diam <- plastics$diameter
xbar<-mean(diam)
s<-sd(diam)
stderror<- s/sqrt(length(diam))
tcritical = -qt(0.005, df=(length(diam)-1))
xbar - tcritical * stderror
xbar + tcritical * stderror
```

### Problem 07
95% Confidence Interval: [299836.7, 299868.1]  
This interval does not include 299729m/s
```{r}
light <- morley$Speed + 299000
xbar<-mean(light)
s<-sd(light)
stderror<- s/sqrt(length(light))
tcritical = -qt(0.025, df=(length(light)-1))
xbar - tcritical * stderror
xbar + tcritical * stderror
```

### Problem 11  
a -> II.  
b -> I.  
c -> III.

### Problem 13  
#### a.
Population mean is 2
```{r}
lambda<-0.5
mu <- (1/lambda)
mu
```
#### b.
```{r}
z<-replicate(10000, {y<-t.test(rexp(10,lambda),mu=2)$conf.int;
      (mu >= y[1]) & (mu <=y[2])})
sum(z)/length(z)

```
#### c.
The value is different than 95% because it is pulling a very small sample, which can lead to very skewed means. Upping the sample size (not the replicate trials) increases the accuracy, while the larger number of trials increases precision.  
By reducing the replicate trials by a factor of ten and increasing the sample size by the same, we start seeing more accurate but less precise results.

```{r}
z<-replicate(1000, {y<-t.test(rexp(100,lambda),mu=2)$conf.int;
      (mu >= y[1]) & (mu <=y[2])})
sum(z)/length(z)
```

#--- markdown-demo.Rmd ---#
---
title: "Markdown demo"
author: "Dr. Clair"
date: "`r format(Sys.Date(), "%B %d, %Y")`"
output:
  pdf_document: default
  html_document: default
---

```{r setup, echo=FALSE}
# This line makes your figures smaller (you want to do this!)
knitr::opts_chunk$set(fig.width=5, fig.height=3)

# This code loads the dplyr library
# You often want to load libraries at the start of your file
suppressWarnings(suppressMessages(library(dplyr)))
```

This is an R Markdown document.  You can turn it into HTML with the Knit menu in RStudio.
If you are willing to install LaTeX, you can produce a PDF document directly.

## First Section

Want some text? Just type it.

Want a list?

* One thing
* Another thing
* The end

This is **important**, this is not.

LaTeX math formatting is built in, use `$` to start and end math.

Is your favorite function $x^2$?  Mine is $\sqrt{x^2 + 4}$.

Using `\[` and `\]` puts math on its own line:
\[ \sum_1^\infty \frac{1}{1+x^2} \]

## Second Section

You can include R code which will display, run, and display its output.
Type CTRL-Option-I to insert an R code block.

Here's some R code using the built-in `iris` data set.

```{r}
str(iris)
max(iris$Sepal.Length)
```

Here's a plot:

```{r}
iris %>% filter(Species == 'setosa') %>%
  select(Sepal.Length,Sepal.Width) %>% plot()
```


#--- May-05-Homework.Rmd ---#
---
title: 'Homework #10'
author: "Robert Campbell"
date: "15 May 2021"
output:
  pdf_document: default
  html_document: default
---

```{r setup, echo=FALSE, warning=FALSE, message=FALSE}
# This line makes your figures smaller (you want to do this!)
knitr::opts_chunk$set(fig.width=5, fig.height=3)

# This code loads the dplyr library
# You often want to load libraries at the start of your file
library(dplyr)
library(ggplot2)
library(quantreg)
#library(babynames)
#library(tidyr)
#library(stringr)

# Homework Problems:
# Ch 11 Exercises # 2-6, 10, 14-17, 20-25, 28
```


## Chapter 11   
### Problem 02    
  a. 
```{r}
faith_mod<-lm(waiting ~ eruptions, data=faithful)
faith_mod
```
  b.
```{r}
faithful %>%  ggplot(aes(x=eruptions, y=waiting)) + geom_point() +
  geom_smooth(method="lm")

```

  c.
```{r}
predict(faith_mod, newdata = data.frame(eruptions=4.3))
```  

### Problem 03    
  a.
```{r}
j <- ISwR::juul
j<-j %>% filter(tanner==5, age<20)
model <- lm(igf1 ~ age, data=j)
model
```
  b. igf1 = (-38.94)*age + 1135.49
  c. 
```{r}
predict(model, newdata=data.frame(age=16))
```

### Problem 04    
  a. Greater than .03
  b. Between  -.03 and .03
  c. Less than -.03   
  d. Greater than .03   


### Problem 05    
```{r}
d<-datasauRus::datasaurus_dozen
d %>% group_by(dataset) %>% summarize(correlation = cor(x, y, use="complete.obs"),
        xbar=mean(x), sx = (sd(x)/sqrt(142)), ybar=mean(y), sy=(sd(y)/sqrt(142)))
```
   
   They all seem to have pretty similar averages and stderr. X and Y seem to largely be not correlated.   

```{r}
d %>% ggplot(aes(x=x, y=y, color=dataset)) + geom_point() + facet_wrap(vars(dataset))
```
  These graphs are just the shapes described by their names.


### Problem 06    
   General physical activity, income, average hours worked per day. Basically, do museum attendees self-select for lower risk of mortality among known risk factors.


### Problem 10    
  f(b) = sum over i of (yi-B-xi). Take derivative of B with respect to B
-2 sum (yi-B-xi) = -2(sum(yi-xi) - n*B) = -2(ybar - xbar -nB) = 0
ybar-xbar = nB, (ybar-xbar)/n = B


### Problem 14    
  a. Yes. Intuitively, at least, denser urban areas seem to have lower school funding.
  b.
```{r}
a <- carData::Anscombe
cor(a$education, a$young)
```
  c. Slope is significant with p=0.026
```{r}
model <- lm(education ~ young, data=a)
summary(model)$coefficients
```
  d. From residuals, AK is a massive outlier. Updated model shows that the slope is not significantly different from 0.
```{r}
a<-a %>% filter(rownames(.)!="AK")
model <- lm(education ~ young, data=a)
summary(model)$coefficients
```

### Problem 15    
  a. 
```{r}
c <- fosdata::cern
c<-c %>% filter(platform=="Twitter") %>% head()
model<-lm(likes ~ shares, data=c)
plot(model)
```
  b. 
```{r}
l_model<-lm(log(likes) ~ shares, data=c)
plot(l_model)
```
  c. The log model looks a little better, but it is still pretty bad.   
  
  
### Problem 16    
  a.
```{r}
crit <- fosdata::crit_period
crit <- crit %>% filter(locale=="North America")
crit %>% ggplot(aes(x=aoa, y=gjt)) + geom_point()

```
  b. 
```{r}
model <- lm(gjt ~ aoa, data=crit)
model
crit %>% ggplot(aes(x=aoa, y=gjt)) + geom_point() + geom_smooth(method="lm")
```
   
  c. The slope represent the change is score based on the age someone learns a second language   
  d. No, there is no distinct bend in the graph.   
```{r}
plot(model)
```

### Problem 17    
  a.
```{r}
data(engel)
e<-engel
e %>%  ggplot(aes(x=income,y=foodexp)) + geom_point() + geom_smooth(method="lm")
```
   
  b.
```{r}
model<-lm(foodexp ~ income, data=e)
plot(model)
```
   
  c. Scale-location should be flat instead of trending upwards.

### Problem 20    
  P = 1.33*10^19, slope is significant, reject the null.
```{r}
pen <- palmerpenguins::penguins
pen<-pen %>% filter(species=="Gentoo")
model<-lm(body_mass_g ~ flipper_length_mm, data=pen)
summary(model)$coefficients

```

### Problem 21    
  a.   
```{r}
hot <- fosdata::hot_dogs
hot %>% ggplot(aes(x=calories,y=sodium, color=type)) + geom_point()
```
   
  b. Remove poultry the data looks to have 4 separate subgroups. Compare meat and beef instead. sodium = 3.613(calories) - 160.58   
```{r}
hot<-hot %>% filter(type!="Poultry")
model<-lm(sodium ~ calories, data=hot)
model
```
  c. 
```{r}
predict(model, newdata=data.frame(calories=140))

```
   
  d.   
```{r}
predict(model, newdata=data.frame(calories=140), interval="predict")

```

### Problem 22    
  a&b.
```{r}
bat<-Lahman::Batting
dat<-bat %>% group_by(yearID) %>% summarize(HR=sum(HR), R=sum(R))
dat %>% ggplot(aes(x=HR, y=R, color=yearID)) + geom_point()
```
  c.   
```{r}
dat<-dat %>% filter(yearID>1969)
dat %>% ggplot(aes(x=HR, y=R, color=yearID)) + geom_point()
```
   
  d. The slope predicts 2.54 runs for each homerun. The slope is significant with pval: 1.359*10^-23.
```{r}
model<-lm(R ~ HR, data=dat)
model
summary(model)$coefficients
```
   
  e. 
```{r}
dat %>% ggplot(aes(x=HR, y=R, color=yearID)) + geom_point() + geom_smooth(method="lm")
```
  f. ~19315, it would not be valid for predicting 1870 data. As we saw, there was a significant change in the relationship between HR and R around 1970.
```{r}
predict(model, newdata=data.frame(HR=4000))
```

### Problem 23    
  a.   
  b. Yes, there's a few. One Especially noticeable is one at ~51 stories that is close to 200 (height units?) taller than another 51 story building.   
  c. Not at a glance. They all seem to follow the same general trend.
```{r}
e <- Sleuth2::ex0728
e %>% ggplot(aes(x=Stories, y=Height, color=Year)) + geom_point() + geom_smooth(method="lm")
```
   
### Problem 24    
  a. The shape is roughly exponential, so the log of wine might help
```{r}
e <- Sleuth3::ex0823
e %>% ggplot(aes(x=Wine, y=Mortality)) + geom_point()
e %>% ggplot(aes(x=log(Wine), y=Mortality)) + geom_point()
```
   
  b. Yes, the data suggests there is a correlation between the two
```{r}
model <- lm(Mortality ~ log(Wine), data=e)
summary(model)$coefficients
cor(log(e$Wine), e$Mortality)

```
   
  c. No, because correlation does not imply causation.


### Problem 25    
```{r}
x <- runif(30, 0, 10)
epsilon <- rnorm(30)  # std normal, mean 0 sd 1
y <- x^2 + epsilon
plot(x,y)
mod <- lm(y ~ x)

# predict at x=5
pi <- predict(mod, data.frame(x=5), interval="predict")
rate<-replicate(10000000, {xnew <- 5;
ynew <- xnew^2 + rnorm(1);
# check the prediction
(pi[2] < ynew & pi[3] > ynew)})
mean(rate)

# predict at x=10
pi <- predict(mod, data.frame(x=10), interval="predict")
rate<-replicate(10000000, {xnew <- 10;
ynew <- xnew^2 + rnorm(1);
# check the prediction
(pi[2] < ynew & pi[3] > ynew)})
mean(rate)
```


### Problem 28    
  a&b.   
```{r}
x = seq(0,10,length.out = 21)

test<- replicate(10000, {epsilon <- rnorm(21, sd=3);
y = 1 + 2*x + epsilon;
model<-lm(y~x);
y[x==5.5] - predict(model, newdata=data.frame(x=5.5))})

sd(test)
```
   
  c.
```{r}
x = seq(0,10,length.out = 21)

test<- replicate(10000, {epsilon <- rnorm(21, sd=3);
y = 1 + 2*x + epsilon;
model<-lm(y~x);
y[x==10] - predict(model, newdata=data.frame(x=10))})

sd(test)
```


#--- May-10-Homework.Rmd ---#
---
title: 'Homework #11'
author: "Robert Campbell"
date: "10 May 2021"
output:
  pdf_document: default
  html_document: default
---

```{r setup, echo=FALSE, warning=FALSE, message=FALSE}
# This line makes your figures smaller (you want to do this!)
knitr::opts_chunk$set(fig.width=5, fig.height=3)

# This code loads the dplyr library
# You often want to load libraries at the start of your file
library(dplyr)
library(ggplot2)
#library(babynames)
#library(tidyr)
#library(stringr)

# Homework Problems:
# Ch 12 Exercises # 4ab, 5, 6, 8, 9, 15
```


## Chapter 12   
### Problem 04   
  a. Looks like the data satisfies ANOVA assumptions
```{r}
pen<-palmerpenguins::penguins
boxplot(bill_depth_mm ~ species, data=pen)
```
   
  b. p-value < 2.2*(10^-16), showing significance at the alpha = 0.01 level.
      Reject the null, species does significantly predict bill depth.
```{r}
depth_mod<-lm(bill_depth_mm ~ species, data=pen)
anova(depth_mod)
```


### Problem 05  
  P-value: 0.00362, reject the null. There are significant differences
in the predictions of 300g objects by age group.
```{r}
anova(lm(mean300 ~ age, data=fosdata::weight_estimate))
```

### Problem 06  
  a. Spock's Lawyers may have a point, as Spock's mean percentage is visibly less than the means of the other 7 lawyers.     
```{r}
judge_perc <- Sleuth3::case0502
boxplot(Percent ~ Judge, data=judge_perc)
```
  b. P-value: 0.3239, do not reject the null. The means of the percentage of women jurors in the 6 other judges courts are significantly similar.   
```{r}
judge_perc %>% filter(Judge != "Spock's") %>% lm(Percent ~ Judge, data=.) %>% anova()
```
  c. The significantly similar means of the other judges justifies comparing Spock's judge to the other 6.
    P-value: 1.303 * (10^-6), reject the null. There is significant difference in the expected percentage of women jurors in Spock's judge's court when compared to the other judges.

```{r}
judge_perc %>% mutate(Spock = (Judge=="Spock's")) %>%
  t.test(Percent ~ Spock, data=.) 

```


### Problem 08  
  The p-value: 5.93*(10^-10) looks to show significant difference in weight based on feed, however the bloxplot shows inconsistent variance in the different groups with the most extreme being when you compare "meatmeal" with "sunflower".   

```{r}
anova(lm(weight ~ feed, data=chickwts))
boxplot(weight ~ feed, data=chickwts)
```

### Problem 09  
  With P-value: 0.0004827, it looks like there is significant difference in the mean based on experiment number. However, looking at the boxplots the data does not satisfy the anova assumptions, especially experiments 3 and 5.   
```{r}
anova(lm(Speed ~ Expt, data=morley))
boxplot(Speed ~ Expt, data=morley)
```

### Problem 15  
  a.   
```{r}
flint_pb <- fosdata::flint
boxplot(log(Pb1) ~ Ward, data=flint_pb)
flint_pb<- flint_pb %>% filter(Ward != 0)
boxplot(log(Pb1) ~ Ward, data=flint_pb)
```
  b. The log levels look normally distributed and the variances a similar enough to justify anova.   
  c. P-value: 0.03137, there is a significant difference in the mean of the log of the amount of lead in the water based on ward in Flint, MI. 
```{r}
anova(lm(log(Pb1) ~ Ward, data=flint_pb))
```


#--- thq-1.Rmd ---#
---
title: "Take Home Quiz 1"
author: "Your Name"
date: "Due Monday Feb 15 at 1:00pm"
output:
  pdf_document: default
  html_document: default
---

This quiz should take you approximately 25 minutes. 
Place your answers into this markdown document, knit it, and hand in the result as a PDF.
You may use R, the internet, and any reference material,
but do not work together and do not get help (except from Dr. Clair).

### Problem 1
Use R to calculuate the sum $\sqrt{1} + \sqrt{2} + \sqrt{3} + \dots + \sqrt{1000}$

### Problem 2
This problem uses the vector `islands` which is built in to R.

a. Make a histogram of the landmass sizes.

b. What is the median size of these large landmasses?

c. What is the average size of these large landmasses?

d. What is the total area of all landmasses in this data set?

### Problem 3
This problem uses the data set `chimps` from the `fosdata` package.

a. How many observations are in this data set?

b. How many variables are in this data set?

c. How many male and female chimpanzees are there?

d. What is the name of the oldest chimpanzee?

### Problem 4
Blood types O, A, B, and AB \index{blood type} have the following probabilities in the United States:

Type        |   O  |   A  |  B   |  AB
------------|------|------|------|------
Probability | 0.45 | 0.40 | 0.11 | 0.04

a. What is the probability that a randomly selected person is *not* type O.

b. What is the probability that a randomly selected person is either type B or AB?

c. If you select 10 people at random, what is the probability that at least one of them is type AB?

## Problem 5
Roll two ordinary six-sided dice.

a. What is the probability that the sum of the dice is 6?

b. What is the probability that at least one of the dice is 4?

c. What is the probability that the sum of the dice is 6, given that at least one of the dice is a 4?



#--- thq-2.Rmd ---#
---
title: "Take Home Quiz 2"
author: "Robert Campbell"
date: "Due 1:00pm Monday, March 29"
output: pdf_document
---

```{r setup, message=FALSE, warning=FALSE}
library(dplyr)
library(ggplot2)
library(babynames)
library(stringr)
```

This quiz should take you approximately 25 minutes. 
Place your answers into this markdown document, knit it, and hand in the result as a PDF.
Just answering is not enough - you need to include the R code that produces your answer.

You may use R, the internet, and any reference material,
but do not work together and do not get help (except from Dr. Clair).

### Problem 1
This problem uses the `babynames` data from the `babynames` library.

a. Find the most popular girl's name in the year 2000.
  Emily
```{r}
babynames %>% filter(sex=="F", year==2000) %>% arrange(desc(n)) %>% head()
```

b. Find the most popular girl's name in the year 2000 that starts with "Q".
  Quinn
```{r}
babynames %>% filter(sex=="F", year==2000) %>% filter(str_detect(name,"^Q"))%>%
  arrange(desc(n)) %>% head()
```

### Problem 2
Continue using `babynames`.
Not all babies are counted in this data set - it only includes names that are given to five or more babies.
The `prop` variable gives the percentage of all babies born that year with the given name.

a. What percentage of all female babies born in 2000 are included in this data?  (Add up the `prop` variable for all female babies born in 2000.)
  91%
```{r}
babynames %>% filter(sex=="F", year==2000) %>% summarise(population = sum(prop))
```
b. How many total female babies born in 2000 are included in this data?
  1,815,110
```{r}
babynames %>% filter(sex=="F", year==2000) %>% summarise(population = sum(n))
```
c. Use parts a and b to estimate the total number of female babies born in 2000 in the U.S.
  1,994,626
```{r}
babynames %>% filter(sex=="F", year==2000) %>% summarise(population = (sum(n)/sum(prop)))
```

### Problem 3
The data set `storms` is included in the `dplyr` package.  It contains information about 198 tropical storms.

a. Use ggplot to produce a histogram of the `wind` speeds in this data set.
Fill your bars using the category variable so you can see the bands of color corresponding to the different storm categories.
```{r}
storms <- dplyr::storms
storms %>% ggplot(aes(x=wind,fill=category)) + geom_histogram()
```
b. Repeat part (a) but make a histogram of the `pressure` variable.  You should observe that high category storms have low pressure.
```{r}
storms %>% ggplot(aes(x=pressure,fill=category)) + geom_histogram()

```

### Problem 4
Use ggplot to produce a plot showing the position track of each storm from 2014 (use `long` for x and `lat` for y).
Color your points by the name of the storm so you can distinguish the seven storm tracks.
Which storm in 2014 made it the furthest North?
  Gonzalo
```{r}
storms %>%filter(year==2014) %>% ggplot(aes(x=long, y=lat,color=name)) + geom_point()
```

### Problem 5
The `ecars` data set from `fosdata` gives information about electric car charging sessions.

Create a visualization showing seven scatterplots with the `chargeTimeHrs` variable on the x axis and the
`kwhTotal` variable on the y axis.  Facet your visualization with one plot per day of week, in the correct day order.

There is one outlier with a very high charge time that you should remove.

```{r}
ecars <- fosdata::ecars
ecars %>% filter(chargeTimeHrs != max(chargeTimeHrs)) %>%
  ggplot(aes(x=chargeTimeHrs,y=kwhTotal, color=weekday)) + geom_point() +
  facet_wrap(~factor(weekday,levels=c("Sun","Mon","Tue","Wed","Thu","Fri","Sat")))
```


#--- thq-3.Rmd ---#
---
title: "Take Home Quiz 3"
author: "Robert Campbell"
date: "Due 1:00pm Friday, May 7"
output: pdf_document
---

```{r setup, include=FALSE, message=FALSE,echo=FALSE,warning=FALSE}
library(dplyr)
library(ggplot2)
```

This quiz should take you approximately 25 minutes. 
Place your answers into this markdown document, knit it, and hand in the result as a PDF.
Just answering is not enough - you need to include the R code that produces your answer.

You may use R, the internet, and any reference material,
but do not work together and do not get help (except from Dr. Clair).

### Problem 1
A company is receiving a shipment of parts.  They randomly select a sample and check that the parts are made to specifications.  If they find that the sample don't meet specifications, they return the whole shipment.

a. What is the null hypothesis for this test?
  That all of the parts in the shipment meets specifications.   
  
b. What would a type I error be for this test?  What problems could a type I error cause?
  That all parts in the shipment meet specification, but the test showed that they did not.   
  This would cause good parts to be returned or scrapped and new parts need to be produced to fill the order, generating unnecesarry waste.   
  
c. What would a type II error be for this test?  What problems could a type II error cause?
  That not all parts in the shipment meet specification, but the test showed that they did.   
  This would cause the shipment to go to market or production with bad parts. Depending on the part that could be a minor but acceptable risk (some cheap product, like a toy) or it could be some critical part and the risk would bwe unacceptable (faulty ignition in a car).   

### Problem 2

On episode 2 of the Nicolas Cage documentary "The History of Swear Words," six comedians took part in an experiment.
Each was asked to submerge their hand in a bucket of ice water for as long as they could stand it.  Four of the comedians were allowed to
swear out loud while performing the experiment, the other two were forbidden from swearing.  The two clean subjects
spent 53 and 58 seconds in the water, while the four swearing comedians spent 69, 78, 87, and 140 seconds in the water.

```{r}
test<- data.frame(time=c(53, 58, 69, 78, 87, 140), swear<- c(0, 0, 1, 1, 1, 1))
```

a. Test for a difference between the two groups using a Wilcoxon rank sum test and report your $p$-value.   
  p-value = 0.1333

```{r}
wilcox.test(time~swear, data=test)
```

b. Repeat your analysis with a $t$-test.   
  p-value = 0.09572
```{r}
t.test(time~swear, data=test)
```

c. Were either of your analyses significant at the $\alpha = 0.05$ level?  What conclusions would you draw from this experiment?   
  No, neither were. The correlation between swearing and time submerged in water is not significant in this sample.

### Problem 3

Suppose a population is normally distributed with mean 25 and standard deviation 7.

We want to test $H_0: \mu = 20$ against $H_a: \mu \neq 20$ at the $\alpha = 0.05$ level of significance.

a. What power would a $t$-test have with a sample of size $n=20$?   
  Power = 0.8576   
```{r}
power.t.test(n=20, delta=20-25, sd=7, type="one.sample")
```

b. What sample size would you need for a power of 95%?
  n is approximately 28 (assuming integers).
```{r}
power.t.test(delta=20-25, sd=7, power=0.95, type="one.sample")
```

### Problem 4
Problems 4 and 5 both use data on oak tree acorns from Marcelo & Patterson, 1990.
The authors suggest that larger acorns allow for greater success in ecological niches, leading to a wider geographic range.

Data is available on our course data page, with `Range` giving the geographic range in $km^2$ x 100 and `Acorn_size` giving the acorn size in $cm^3$.

```{r cache=TRUE}
acorns <- read.csv("https://mathstat.slu.edu/~clair/stat/data/acorns.csv")
```

Make a scatterplot showing Range as explained by Acorn_size, and add the regression line to your plot.
```{r}
range_mod<-lm(Range ~ Acorn_size, data=acorns)
range_mod
acorns %>% ggplot(aes(x=Acorn_size, y=Range)) + geom_point() +
  geom_abline(slope=1218, intercept=7399)
```

What is the equation of the regression line?
  y: Range, x: Acorn_size   
  y = 7399 + 1218x   

### Problem 5

Continue working with acorns.

a. Predict the geographic range for an oak tree with acorn size 3.6 $cm^3$.
```{r}
predict(range_mod, newdata = data.frame(Acorn_size=3.6))
```

b. Test for a linear relationship between acorn size and geographic range.  Report a p-value with your conclusion.   
  There is a linear relationship between acorn size and geographic range with a p-value of 0.02384.
```{r}
summary(range_mod)
```

