1)
Robert Campbell

2)
It echoes what is typed on a line when enter is pressed

3)
It prints the text to the terminal

4)
It wrote the 3 lines of text that I typed to the new.txt file.

5)

6)
unistd.h

7)

8)
ssize_t (signed integer), which represents the number of bytes read.
A 0 indicates end of file.

9)
Yes, the behavior matches.

10)
It will not effect the "correctness" as it will still perform its function, as long as it has a buffer.

11)
The smaller the buffersize the less memory is needed for the program to run, but it will require more iterations.
A larger buffer size will require fewer loops, but require more memory to store the buffer in.